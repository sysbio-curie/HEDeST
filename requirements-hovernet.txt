# Environment for the HoVer-Net stages (tissue mask and nuclei segmentation).
#
#   conda create -y -n hovernet-env -c conda-forge python=3.8 "openslide>=3.4.1"
#   conda run -n hovernet-env pip install -r requirements-hovernet.txt
#
# or simply: ./setup_env.sh
#
# HoVer-Net is kept apart from hedest-env because it needs Python 3.8 and its own
# torch build. The pipeline calls it as a subprocess: point 'segmentation.python'
# (and 'mask.python') of the pipeline configuration at this environment.
#
# This covers inference only, which is what the pipeline uses. Training HoVer-Net
# additionally needs scikit-learn and tensorboardX.

--extra-index-url https://download.pytorch.org/whl/cu118

# deep learning
torch==2.4.1+cu118
torchvision==0.19.1+cu118

# scientific stack
numpy==1.24.4
pandas==2.0.3
scipy==1.10.1
scikit-image==0.21.0
imageio==2.35.1
networkx==3.1

# slides and images
openslide-python==1.3.1
opencv-python==4.10.0.84
pillow==10.4.0
tifffile==2023.7.10
shapely==2.0.6
imgaug==0.4.0

# HoVer-Net runtime
docopt==0.6.2
psutil==6.0.0
termcolor==2.4.0
matplotlib==3.7.5
tqdm==4.66.5

# used by the HEDeST wrappers around HoVer-Net
anndata==0.9.2
typer==0.15.1
loguru==0.7.3

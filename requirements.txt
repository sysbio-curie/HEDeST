# Environment for the HEDeST pipeline: slide handling, H-Optimus-0 feature
# extraction, model training and analysis.
#
#   conda create -y -n hedest-env -c conda-forge python=3.9 "openslide>=3.4.1"
#   conda run -n hedest-env pip install -r requirements.txt
#
# or simply: ./setup_env.sh
#
# Python 3.9 on linux-x86_64 is required: the torch-scatter wheel below is built
# for cp39. HoVer-Net needs its own environment, see requirements-hovernet.txt.

--extra-index-url https://download.pytorch.org/whl/cu116

# deep learning
torch==1.13.1+cu116
torchvision==0.14.1+cu116
torch_scatter @ https://data.pyg.org/whl/torch-1.13.0%2Bcu116/torch_scatter-2.1.1%2Bpt113cu116-cp39-cp39-linux_x86_64.whl
timm==1.0.14

# scientific stack
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1
scikit-image==0.24.0
networkx==3.2.1

# spatial transcriptomics
anndata==0.10.8
scanpy==1.10.2

# slides and images
openslide-python==1.3.1
opencv-python==4.10.0.84
pillow==10.4.0
tifffile==2024.8.24
shapely==2.0.7

# figures
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.23.0

# files, CLI, logging
PyYAML==6.0.2
openpyxl==3.1.5
XlsxWriter==3.2.3
typer==0.15.1
loguru==0.7.3
tqdm==4.66.5
joblib==1.4.2
pyinstrument==5.0.1

# Optional: pyvips makes the conversion to pyramidal TIFF much faster. It needs
# the libvips system library, so it is not installed by default.
# pyvips==2.2.3
